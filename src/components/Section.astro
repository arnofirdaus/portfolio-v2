---
interface Props {
  theme?: 'bone' | 'obsidian';
  class?: string;
  id?: string;
}
const { theme = 'bone', class: className, id } = Astro.props;
---
<section id={id} class={`theme-${theme} w-full scroll-mt-16 ${className || ''}`}>
  <slot />
</section>
